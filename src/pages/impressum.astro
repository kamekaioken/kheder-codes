---
import { getEntry } from 'astro:content';
import LegalDocView from '../components/views/LegalDocView.astro';

const entry = await getEntry('legal', 'impressum');
if (!entry) throw new Error('Missing legal document: impressum');
---

<LegalDocView locale="de" {entry} />
